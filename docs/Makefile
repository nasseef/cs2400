# Makefile for the program test.cc
# Change the name to your program name
# Change the flags below if you need to.
# The flags are set for detecting all warnings,
# using C++11, and debugging. 

CC = g++
CFLAGS = -Wall -std=c++11 -g

# target executable
a.out: test.cc
	$(CC) $(CFLAGS) test.cc

clean: 
	rm -f a.out
